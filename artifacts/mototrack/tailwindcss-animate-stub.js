// Tailwind v4 MotoTrack uses tw-animate-css via CSS @import — tailwindcss-animate is
// not needed here. This stub satisfies the import when @tailwindcss/vite loads the
// workspace-root tailwind.config.ts into Vite's module graph for HMR watching.
export default function tailwindcssAnimateStub() {
  return {};
}
