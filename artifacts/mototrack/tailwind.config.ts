// Tailwind v4: all configuration lives in src/index.css via @theme / @plugin directives.
// This file exists only to prevent @tailwindcss/vite from crawling up to the workspace
// root and loading the root tailwind.config.ts (which requires packages not available here).
export default {};
